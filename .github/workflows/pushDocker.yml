name: pushDocker
on:
  push:
    branches:
      - main
  schedule:
    - cron: 45 23 * * 0 # https://crontab.guru/#45_23_*_*_0
jobs:
  test:
    name: Test Docker
    permissions:
      contents: read
    uses: jaid/workflows/.github/workflows/testDocker.yml@main
    with:
      test: result.stdout.includes('+0000 cargo')
  build:
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - flavorSuffix: debian
            image: debian:stable-slim
          - image: ubuntu:rolling
          - flavorSuffix: lts
            image: ubuntu:latest
    uses: jaid/workflows/.github/workflows/pushDocker.yml@main
    with:
      dockerHubUser: jaidchen
      buildArgs: |
        image=${{ matrix.image }}
        flavorSuffix=${{ matrix.flavorSuffix }}
      flavorSuffix: ${{ matrix.flavorSuffix }}
    secrets:
      dockerHubToken: ${{ secrets.dockerHubToken }}
